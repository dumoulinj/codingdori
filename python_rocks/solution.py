class Solution:
    def __init__(self):
        self.score = 0


    def write_result(filename, solution):
        """
        Write result to output file
        """
        f = open(filename, 'w')
        # f.write(str(len(newslices)) + "\n")
        # for slice in newslices:
        #    f.write("{} {} {} {}\n".format(slice[0], slice[1], slice[2], slice[3]))


    def compute_score(self):
        """
        Compute and return the score
        """
        score = 0

        # ...

        return score